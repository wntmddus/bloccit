<h1><%= @question.title %></h1>
<h1><%= @question.body %></h1>
<h1><%= @question.resolved %></h1>


<div class="col-md-4">
<!-- #5 -->
 <%= link_to "Edit", edit_post_path(@question), class: 'btn btn-success' %>
 <%= link_to "Delete Post", @question, method: :delete, class: 'btn btn-danger',
 data: { confirm: 'Are you sure you want to delete this question?'} %>
</div>
